/*public class e5_7 {
    public static boolean anagrams(String a, String b){
        QueueObj SA = new QueueObj(a.length());
        QueueObj SB = new QueueObj(b.length());

        for (int i = 0; i<a.length(); i++){
            SA.enqueue(a.charAt(i));
        }

        for (int i = 0; i<b.length(); i++){
            SB.enqueue(b.charAt(i));
        }

        while (!SA.isEmpty() && !SB.isEmpty()){

        }
    }
}*/
